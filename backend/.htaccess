<IfModule mod_rewrite.c>
  RewriteEngine On

  # If this folder is published as /api (or /staging/api), route non-files to index.php
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  RewriteRule ^ index.php [L]
</IfModule>

