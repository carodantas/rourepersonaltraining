export const environment = {
  production: true,
  /** Same-origin when the CMS lives on this dashboard host (rare); otherwise set main site HTTPS origin */
  apiBaseUrl: '',
  /** Public SPA origin for preview links (main site); empty = reuse dashboard origin */
  sitePublicOrigin: '',
};

